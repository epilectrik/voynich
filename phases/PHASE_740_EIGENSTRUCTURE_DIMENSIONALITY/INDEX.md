# PHASE 740 — Currier B macro-eigenstructure: dimensionality, no-cyclic-mode, and the characterization echo-wall

**Status:** COMPLETE — 1 constraint registered (C2067); extensive characterization is negative-knowledge / echo (not registered)
**Date:** 2026-06-01
**Type:** Off-books exploratory characterization of the C2061 above-Markov eigenstructure; registration adjudicated by 3-expert differential.
**Verdict:** The eigenstructure is ≥2-dimensional above-Markov with no genuine cyclic mode (REGISTERED, C2067). Every attempt to characterize *what* the modes are either failed or re-derived existing framework (NOT registered — echo).

---

## What was registered (C2067, Tier 2)

The raw-49 class-transition operator's above-Markov slow eigenstructure is **≥2-dimensional** — both λ2 (+0.206, per-synth-own-shuffle 5-gram excess z=+2.5, reproduces C2061 exactly) and λ3 (−0.134, z=+4.4, *sharper* than λ2) survive; **λ4 (+0.088) is marginal (p=0.025, shape-stability 0.56, flips complex 62%) and NOT claimed**. There is **no genuine cyclic/rotational eigenmode**: complex pairs (~12-step) are 5-gram artifacts (lead-|imag| excess p=0.45), and the recurrence curve has no periodic peak (peak-vs-Markov-1 z=+0.82, p=0.19). Extends C2061 (λ2-only) and is scope-corrected to a POOLED-corpus property (does not decompose to sections at current N).

## What was NOT registered, and why

| Attempt | Result | Disposition |
|---|---|---|
| Per-layer carrier vocabulary (which classes carry each mode) | **REFUTED** — ablation frequency-confounded; the frequency control was run on the WRONG vector (participation vs ablation); contradicts C2065 (distributed mode). | Negative-knowledge + memory (wrong-vector-control trap) |
| Per-section eigenmode localization | **UNDERPOWERED** — per-section subspaces degenerate / non-reproducible (Herbal ≈ random). | Negative-knowledge; C2061 scope-noted as pooled-only |
| λ2 = "complexity/e-depth axis" | λ2 loads on **general token length** (non-e length = e-count; e-correlation is length-collinearity, NOT e-specific). | DO-NOT-REGISTER label (echo ≈ C1430); the e-depth-collinearity **negative** logged (protects C1225) |
| λ3 = "the h-axis" | λ3 loads cleanly on h-content (+0.49) — recorded as metadata on C2067, NOT as an operational claim. | DO-NOT-REGISTER gloss (echo ≈ C089; C171-capped) |
| "Spec not trace / loop is in the referent" | The no-return measurement is real (= C2067's no-cyclic-mode), but the reading re-derives C1399/C1400/C1429 paragraph/line independence. | DO-NOT-REGISTER (echo; structure↔referent, C171, can't be same-model-cleared). Measurement is in C2067. |
| The rotation (~12-step cycle) | 5-gram artifact. | Captured as C2067's no-cyclic-mode |

## The methodological lesson (the echo-wall)

At this project maturity, eigenstructure *interpretation* re-derives the framework: λ2≈C1430, λ3≈C089, spec-not-trace≈C1399. The modes are distributed (C2065), so they do not decompose into a *new* clean meaning — they project onto axes the framework already has. The genuinely new content was the **measurement** (dimensionality + no-cyclic-mode); the meaning was echo. Lessons saved to memory.

## Off-books scripts (not retained — exploratory)
Richness (λ2/3/4 5-gram), interaction (real/complex spectrum), carrier dissection (ablation+participation), bootstrap stability, sequence-projection (position/A-B), e-vs-length decomposition, HT-inclusion check, recurrence/spec-not-trace (5-gram + Markov-1 + peak test). All reproduced the C2061 pipeline; verdicts above.

## Cross-references
C2061 (extended), C2062, C2065 (corroborated), C976-C978, C1010, C1225 (protected), C610/C740 (HT-tail cross-ref), C1430/C089/C1399 (echo, not re-registered), Tier 0 (untouched).
